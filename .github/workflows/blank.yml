name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        repository: 'DevExpress/devextreme-reactive'
        path: devextreme-reactive
    - name: Run yarn
      run: yarn
    - name: Build sources
      run: yarn build
